# считывание настроек с помощью spring cloud config
spring.config.import=configserver:http://localhost:8888

# эту настройку лучше не выносить из приложения, иначе в spring cloud название может быть UNKNOWN
# id приложения - желательно указывать всегда в микросервисных приложениях
spring.application.name=planner-users

spring.profiles.active=micro, users, logging
# spring.profiles.active=micro, users, logging, rabbit, kafka

# TODO позже вынести в cloud-config (пока лень да и пусть будет в истории коммитов)
# Настройки подключения к keycloak
keycloak.auth-server-url=http://localhost:8180/
# Созданный пользоватоель
keycloak.resource=my-manage-todoapp-client-id
keycloak.realm=my-todoapp-realm
# ssl е потребуется для внутренних вызовов (локал разработка)
keycloak.ssl-required=external
# локал разработка, можно коммитить креды
keycloak.credentials.secret=NeVXBgD0xVmK8k8JeO3FfIQZUup9xkt7
keycloak.use-resource-role-mappings=true



# Ссылка на сертификаты auth server для проверки целостности access token
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8180/realms/my-todoapp-realm/protocol/openid-connect/certs